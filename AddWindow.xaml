<Window x:Class="coh2ui.AddWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:local="clr-namespace:coh2ui"
        xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
        KeyDown="Window_KeyDown"
        MouseDown="Window_MouseDown"
        mc:Ignorable="d"
        Title="EditWindow" Height="250" Width="400">
    <Window.Resources>
        <DataTemplate x:Name="KeyBind" DataType="{x:Type local:Key}">
            <StackPanel Orientation="Horizontal">
                <Border BorderBrush="Gray" BorderThickness="1" CornerRadius="3" Padding="4" Margin="2">
                    <TextBlock Text="{Binding}" />
                </Border>
            </StackPanel>
        </DataTemplate>
    </Window.Resources>
    
    
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="225*"></ColumnDefinition>
            <ColumnDefinition Width="225*"></ColumnDefinition>
        </Grid.ColumnDefinitions>
        
        <Border Grid.Column="0" Background="#213e24"></Border>
        <Border Grid.Column="1" Background="#213e24"></Border>
    
        <TextBlock
            Grid.Column="0"
            Grid.ColumnSpan="2"
            Panel.ZIndex="220"
            VerticalAlignment="Center"
            HorizontalAlignment="Center"
            >
            <materialDesign:PackIcon Kind="ArrowForward" Width="60" Height="60" Foreground="White" />
        </TextBlock>
        
        <Border Grid.Column="0"
                CornerRadius="5"
                Margin="10"
                Background="#101f12"
                >
            <TextBlock
                x:Name="LeftAddBlock"
                VerticalAlignment="Center"
                HorizontalAlignment="Center"
                Foreground="White"
                FontSize="40">
                From
            </TextBlock>
        </Border>
        <Border Grid.Column="1"
                CornerRadius="5"
                Margin="10"
                Background="#101f12"
                >
            <TextBlock
                x:Name="RightAddBlock"
                VerticalAlignment="Center"
                HorizontalAlignment="Center"
                Foreground="White"
                FontSize="40">
                To
            </TextBlock>
        </Border>
        
    </Grid>
</Window>
