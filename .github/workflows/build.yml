name: Build COH2UI

on:
  push:
    branches: [ "*" ]
  pull_request:
    branches: [ "*" ]

jobs:
  build:
    runs-on: windows-latest

    steps:
      - name: 🧾 Checkout repository
        uses: actions/checkout@v3

      - name: 🛠 Setup .NET
        uses: actions/setup-dotnet@v4
        with:
          dotnet-version: '8.0.x'

      - name: 📦 Restore NuGet packages
        run: dotnet restore ./coh2ui.csproj

      - name: 🏗️ Build coh2ui (без WAP)
        run: dotnet build ./coh2ui.csproj --configuration Release --no-restore

      - name: 🚀 Publish coh2ui
        run: dotnet publish ./coh2ui.csproj --configuration Release --output ./publish --no-restore
      
      #     - name: ⬆️ Upload artifact # ненавижу гитхуй
      #        uses: actions/upload-artifact@v3.1.3
      #        with:
      #          name: coh2ui-build
      #          path: ./publish

      - name: 📂 Output files in ./publish
        run: dir ./publish